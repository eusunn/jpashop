spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #기본적으로 create-drop으로 처리가 됌
#    properties:
#      hibernate:
#        #show_sql: true
#        format_sql: true
# 스트링 부트에서는 test를 돌릴 때 위에 줄들이 다 없으면 알아서 메모리 모드로 돌리게 된다.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace